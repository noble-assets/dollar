syntax = "proto3";

package noble.dollar.portal.v1;

option go_package = "dollar.noble.xyz/types/portal";

// 
// Note: this is a special message type that is only for internal ABCI++ usage
// for inserting VAA's into the block
message MsgDeliverInjection {
  bytes vaa = 1;
}
